using System;

namespace Warden.Messages.Events.Spawn
{
    public class SpawnWardenRejected : IRejectedEvent
    {
        public Guid RequestId { get; }
        public string UserId { get; }
        public string Code { get; }
        public string Reason { get; }

        protected SpawnWardenRejected()
        {
        }

        public SpawnWardenRejected(Guid requestId, 
            string userId, string code, string reason)
        {
            RequestId = requestId;
            UserId = userId;
            Code = code;
            Reason = reason;
        }
    }
}